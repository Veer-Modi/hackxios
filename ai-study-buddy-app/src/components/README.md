# Components Folder

This folder contains reusable UI components for the AI Study Buddy app.

## Structure:
- `Timer/` - Timer-related components
- `LifeSystem/` - Life display and management components  
- `Dashboard/` - Dashboard and stats components
- `UI/` - Basic UI components (buttons, cards, etc.)